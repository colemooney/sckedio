import React from 'react';
import NavBar from '../../components/NavBar/Navbar';

const Build = () => {
    return (
        <div>
            <NavBar />
            <h1>Build Page</h1>
        </div>
    );
};

export default Build;