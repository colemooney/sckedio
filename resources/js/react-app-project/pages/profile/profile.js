import React from 'react';
import NavBar from '../../components/NavBar/Navbar';

const Profile = () => {
    return(
        <div>
            <NavBar />
            <h1>Profile</h1>
        </div>
    );
};

export default Profile;